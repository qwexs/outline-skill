{
  "_comment": "Multi-instance Outline config. Tokens go in env (OUTLINE_API_TOKEN_<NAME>), not here. Replace baseUrl values with real hosts — never commit real tokens or private hostnames you do not want public.",
  "defaultInstance": "work",
  "instances": {
    "work": {
      "baseUrl": "https://REPLACE_WITH_YOUR_OUTLINE_DOMAIN/api"
    },
    "personal": {
      "baseUrl": "https://REPLACE_WITH_SECOND_OUTLINE_DOMAIN/api"
    }
  }
}
